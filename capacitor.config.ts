import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.codna.mgasiningsamuseo',
  appName: 'mga-sining-sa-museo',
  webDir: 'dist'
};

export default config;
